{{> post-info post}}

{{#if loggedIn}}

    <div
        class=" 
      card
          align
          mx-4
          mt-4
          bg-dark bg-gradient
          text-white
          customCard
        "
      >
        <div class="card-body ">
          <h4 class="card-title">
       
          </h4>
   <hr>
   <form id="comment-form" class="card-body">
    <label class="form-label">Comment</label>
    <div class="input-group mb-3">
      <input id="comment" name="comment-body" type="text" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
    </div>

    <div class="d-grid gap-2">
    <button type="submit" class="btn btn btn-secondary">Submit</button>
    </div>
</form>
        </div>
    </div>

{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}


