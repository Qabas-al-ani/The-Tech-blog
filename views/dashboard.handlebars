{{#if posts.length}}
<section>
  <h4>Your Posts</h4>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</section>
{{/if}}

<div class="d-grid gap-2 col-4 mx-auto">
    <button type="submit" class="btn btn-secondary"><a class="link" href="/dashboard/new">+ New Post</a></button>
</div>

<script src="/js/add-post.js"></script>